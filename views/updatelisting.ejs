<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update - <%= listing.title %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <%- include("./layouts/header") %>

    <div class="bg-zinc-200 min-h-screen flex items-center justify-center py-5">
      <div class="w-full max-w-xl bg-white rounded-2xl shadow-xl p-8">
        <h2 class="text-3xl font-bold text-center text-indigo-700 mb-8">Update Listing</h2>
        <form method="post" action="/<%= listing._id %>/update" class="space-y-5">
          <!-- Title -->
          <div>
            <label for="title" class="block text-sm font-semibold text-gray-700 mb-1">Title</label>
            <input type="text" id="title" name="title" value="<%= listing.title %>"
                   class="w-full border rounded-lg px-3 py-2 bg-zinc-100 focus:ring-2 focus:ring-indigo-400 outline-none" required>
          </div>
          <!-- Description -->
          <div>
            <label for="description" class="block text-sm font-semibold text-gray-700 mb-1">Description</label>
            <textarea id="description" name="description" rows="3"
                      class="w-full border rounded-lg px-3 py-2 bg-zinc-100 focus:ring-2 focus:ring-indigo-400 outline-none" required><%= listing.description %></textarea>
          </div>
          <!-- Image URL -->
          <div>
            <label for="imageUrl" class="block text-sm font-semibold text-gray-700 mb-1">Image URL</label>
            <input type="url" id="imageUrl" name="image"
                   value="<%= listing.image %>"
                   class="w-full border rounded-lg px-3 py-2 bg-zinc-100 focus:ring-2 focus:ring-indigo-400 outline-none" required>
          </div>
          <!-- Price -->
          <div>
            <label for="price" class="block text-sm font-semibold text-gray-700 mb-1">Price</label>
            <input type="number" id="price" name="price" value="<%= listing.price %>"
                   class="w-full border rounded-lg px-3 py-2 bg-zinc-100 focus:ring-2 focus:ring-indigo-400 outline-none" required>
          </div>
          <!-- Location -->
          <div>
            <label for="location" class="block text-sm font-semibold text-gray-700 mb-1">Location</label>
            <input type="text" id="location" name="location" value="<%= listing.location %>"
                   class="w-full border rounded-lg px-3 py-2 bg-zinc-100 focus:ring-2 focus:ring-indigo-400 outline-none" required>
          </div>
          <!-- Update Button -->
          <div class="pt-2">
            <button type="submit" class="w-full bg-yellow-500 text-white py-3 rounded-xl font-bold text-lg shadow-md hover:bg-yellow-600 transition-all duration-200">Update Listing</button>
          </div>
        </form>
      </div>
    </div>

    <%- include("./layouts/footer") %>
</body>
</html>